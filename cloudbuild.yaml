steps:
  # 1. Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/inventory-app:$COMMIT_SHA', '.']
  
  # 2. Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/inventory-app:$COMMIT_SHA']
  
  # 3. Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'inventory-app'
      - '--image'
      - 'gcr.io/$PROJECT_ID/inventory-app:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--allow-unauthenticated'
      - '--update-env-vars'
      - 'INSTANCE_CONNECTION_NAME=nicoservertest:us-central1:guias-locales-prod,DB_HOST=34.55.226.178'

images:
  - 'gcr.io/$PROJECT_ID/inventory-app:$COMMIT_SHA'
